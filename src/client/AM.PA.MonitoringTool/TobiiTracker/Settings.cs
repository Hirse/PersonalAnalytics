namespace TobiiTracker
{
    internal static class Settings
    {
        internal const string FixationContextTable = "tobii_fixation_context";
        internal const string FixationSummaryTable = "tobii_fixation_summary";

        internal const bool Enabled = true;
        internal const bool FixationContextEnabled = true;
        internal const bool FixationSummaryEnabled = true;
    }
}
